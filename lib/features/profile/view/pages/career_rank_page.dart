import 'package:flutter/material.dart';

class CarerRankPage extends StatelessWidget {
  const CarerRankPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('CarerRank Page')));
  }
}
